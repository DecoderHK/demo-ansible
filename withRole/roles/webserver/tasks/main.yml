---

- name: Install nginx
  package:
    name: nginx
    state: present

- name: Deploy nginx site config
  template:
    src: site.conf.j2
    dest: /etc/nginx/conf.d/site.conf
  notify: restart nginx
